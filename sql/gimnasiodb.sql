-- SQL dump (short version). Replace with your full dump if you want exact data.
CREATE DATABASE IF NOT EXISTS gimnasiodb;
USE gimnasiodb;
-- (Add the full dump content you provided into this file on your machine if needed)
